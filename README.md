# terpsi-music

Music workspace for the Terpsi project. Fleet-wired as a **core** Willow project.

Local fleet overlay (`.mcp.json`, `.willow/`, `.cursor/`) is gitignored — materialized by `willow project sync terpsi-music` on the dev machine.
