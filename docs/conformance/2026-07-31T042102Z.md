# Conformance record — 2026-07-31

- **at** `2026-07-31T04:21:02+00:00`
- **commit** `d26d9310352e`
- **outcome digest** `f538d1633c4fa64a`

**7 pass · 0 fail · 7 unknown · 0 absent** of 14 checks.

`UNKNOWN` is not a pass. A record that read all-pass on the day it was first written would be the formality §17 warns about, not a check.

| | check | §17 guarantee | evidence |
|---|---|---|---|
| **PASS** | `no-egress` | core purity: the inner ring cannot reach out | 19 module(s) scanned recursively by AST; no egress import, no dynamic import of one, no process spawn |
| **UNKNOWN** | `write-paths` | declared write paths (§6) | 19 module(s) scanned by AST: no writes, no reads — but the manifest declares no write paths to reconcile against, so this is not evidence the mechanism works |
| **UNKNOWN** | `dated-revocation` | revocation by date, never by delete (§7.1) | 19 module(s): Edge carries valid_at/invalid_at/created_at and nothing deletes standing — but revocation-by-delete is a property of a store and there is no store, so this is narrower than it reads |
| **PASS** | `standalone-suites` | every test file runs standalone and can fail | 34 test file(s): each has a __main__ runner that exits nonzero on failure |
| **PASS** | `ablation` | every guard shown to fail (rule 19) | all 155 guards ablate red; the suites are not decorative |
| **PASS** | `exit-line` | the exit line and its export (§11.1) | docs/EXIT.md, 76 lines |
| **PASS** | `component-map` | §14 says whether anyone looked (item 0) | §14: 10/40 exists-rows verified at source |
| **PASS** | `declared-sockets` | listening sockets are declared (§4.3, item 4 note ii) | 84 source file(s) scanned; 0 listener(s), all declared; 0 outbound |
| **PASS** | `manifest` | manifest with a build-failing cloud-permission check (§6) | 4 surface(s) declared and present, 84 source file(s) reconciled: no listener, no outbound, no cloud permission, no unknown key |
| **UNKNOWN** | `security-audit` | SECURITY_AUDIT.md against the shared rubric (§10) | willow-2.0's 15-check rubric is named in §14 as reusable and has not been run here; UNVERIFIED at source |
| **UNKNOWN** | `sidecar-only` | canonical store read-only; agent writes are sidecar (§5) | there is no store, so the rule cannot be violated or demonstrated |
| **UNKNOWN** | `knock-enforcing` | the knock wired in enforcement mode (§7.2) | session reconciliation lives in willow-gate and is not wired here |
| **UNKNOWN** | `allowlist-rot` | allowlist rot tests (§16) | no destination allowlist exists; §14 records the fleet-wide version as unique to UTETY |
| **UNKNOWN** | `named-middles` | a named middle for every pair the app creates (§16) | not mechanically decidable. The middles named and tested are crossing, standing.is_self_edge, marking.drift, practice._one_lane, and — with the surfaces — scales.drift, render.check and manifest.reconcile; whether that is *every* pair is a reading, not a check |

---

Written by `tools/conform.py`. Records are never overwritten: *"does it conform"* is answerable from any one of them, and *"when did it stop"* only from the series.
