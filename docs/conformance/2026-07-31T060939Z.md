# Conformance record — 2026-07-31

- **at** `2026-07-31T06:09:39+00:00`
- **commit** `94dd8583b93a`
- **outcome digest** `f95509b64bf2f6bd`

**11 pass · 0 fail · 9 unknown · 1 absent** of 21 checks.

`UNKNOWN` is not a pass. A record that read all-pass on the day it was first written would be the formality §17 warns about, not a check.

| | check | §17 guarantee | evidence |
|---|---|---|---|
| **PASS** | `no-egress` | core purity: the inner ring cannot reach out | 23 module(s) scanned recursively by AST; no egress import, no dynamic import of one, no process spawn |
| **UNKNOWN** | `write-paths` | declared write paths (§6) | 23 module(s) scanned by AST: no writes, no reads — but the manifest declares no write paths to reconcile against, so this is not evidence the mechanism works |
| **UNKNOWN** | `dated-revocation` | revocation by date, never by delete (§7.1) | 23 module(s): Edge carries valid_at/invalid_at/created_at and nothing deletes standing — but revocation-by-delete is a property of a store and there is no store, so this is narrower than it reads |
| **PASS** | `standalone-suites` | every test file runs standalone and can fail | 42 test file(s): each has a __main__ runner that exits nonzero on failure |
| **PASS** | `ablation` | every guard shown to fail (rule 19) | all 304 guards ablate red; the suites are not decorative |
| **PASS** | `exit-line` | the exit line and its export (§11.1) | docs/EXIT.md, 76 lines |
| **PASS** | `component-map` | §14 says whether anyone looked (item 0) | §14: 11/41 exists-rows verified at source |
| **UNKNOWN** | `classification-registry` | the class-to-L mapping is enforced in one place (§9 item 2, §16) | 113 of 116 field(s) reconciled (7 elevated by a recorded rule); 3 undecided and named: reconciled_session.declared registry=L4 derives=L3, reconciled_session.diff registry=L4 derives=L3, reconciled_session.observed registry=L4 derives=L3 |
| **PASS** | `declared-sockets` | listening sockets are declared (§4.3, item 4 note ii) | 99 source file(s) scanned; 0 listener(s), all declared; 0 outbound |
| **PASS** | `manifest` | manifest with a build-failing cloud-permission check (§6) | 4 surface(s) declared and present, 99 source file(s) reconciled: no listener, no outbound, no cloud permission, no unknown key |
| **ABSENT** | `key-escrow` | data at rest is sealed and its key escrowed (§5; §10's R16) | no keyring and no sealed store exist here, so no master has a disposition to report; the sealing primitive is usable. §5 leaves the escrow policy open and calls single-file key loss the failure mode that ends the program — records/atrest.py reports ABSENT for any master with none, and that guard is ablated |
| **PASS** | `security-audit` | the fifteen-check rubric run here, as a gate (§10) | SECURITY-AUDIT.md dated 2026-07-31 at `d2817f289b63909e3fde6d198086c23178594273`, 0 day(s) old; 5 finding(s) recorded, 1 open, none at S1 or above |
| **UNKNOWN** | `local-inference` | every inference path routes through the refusal-1 guard (§6) | 38 module(s) scanned by AST and none reaches a model: nothing was checked. The guard (records/inference.py) and this tripwire are wired and shown to fail against tests/fixtures/decoys, so the first inference path is caught on arrival |
| **PASS** | `anchor-payload` | an anchor crosses because it carries nothing else (§5, §6) | three attempts made and refused (a subclass field, an attribute hung on the instance, a publication off the calendar); an ordinary anchor yields 32 bytes |
| **UNKNOWN** | `anchor-published` | anchors are published on the calendar (§18 item 15) | the payload, the register and the pending states exist and are shown to fail (tests/test_publication.py); whether a deployment submits weekly is a property of a seam this tree deliberately does not contain |
| **PASS** | `receipt-attribution` | guardian receipts a third party can attribute (§18 item 15) | issuance schemes available: ed25519, hmac-sha256 |
| **PASS** | `deposit-procedure` | the annual deposit is written down (§18 item 15) | docs/WITNESS-DEPOSIT.md, 147 lines, all 5 required sections present. Whether a deposit was performed is not decidable from a tree |
| **UNKNOWN** | `sidecar-only` | canonical store read-only; agent writes are sidecar (§5) | there is no store, so the rule cannot be violated or demonstrated |
| **UNKNOWN** | `knock-enforcing` | the knock wired in enforcement mode (§7.2) | session reconciliation lives in willow-gate and is not wired here |
| **UNKNOWN** | `allowlist-rot` | allowlist rot tests (§16) | no destination allowlist exists; §14 records the fleet-wide version as unique to UTETY |
| **UNKNOWN** | `named-middles` | a named middle for every pair the app creates (§16) | not mechanically decidable. Seventeen middles are named and tested (crossing, standing.is_self_edge, standing.may_supersede, serving._acting_ward, serving._ceiling, marking.drift, practice._one_lane, publication.reconcile, inference.CLASSES<->SENSITIVITY.md, inference.COVERED_CLASSES<->CLAUDE.md refusal 1, providers.GUARD_ENTRIES<->records.inference, scales.drift, render.check, manifest.reconcile, registry.reconcile<->migrations/001_lanes.sql+records/classify.py, atrest.reconcile, atrest.composes); whether that is *every* pair is a reading, not a check |

---

Written by `tools/conform.py`. Records are never overwritten: *"does it conform"* is answerable from any one of them, and *"when did it stop"* only from the series.
