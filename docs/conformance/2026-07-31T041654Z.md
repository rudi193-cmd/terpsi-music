# Conformance record — 2026-07-31

- **at** `2026-07-31T04:16:54+00:00`
- **commit** `a21caa6e1c1a`
- **outcome digest** `94531a3ec2f2732b`

**7 pass · 0 fail · 10 unknown · 1 absent** of 18 checks.

`UNKNOWN` is not a pass. A record that read all-pass on the day it was first written would be the formality §17 warns about, not a check.

| | check | §17 guarantee | evidence |
|---|---|---|---|
| **PASS** | `no-egress` | core purity: the inner ring cannot reach out | 20 module(s) scanned recursively by AST; no egress import, no dynamic import of one, no process spawn |
| **UNKNOWN** | `write-paths` | declared write paths (§6) | 20 module(s) scanned by AST: no writes, no reads — but no manifest exists to declare against, so this is not evidence the mechanism works |
| **UNKNOWN** | `dated-revocation` | revocation by date, never by delete (§7.1) | 20 module(s): Edge carries valid_at/invalid_at/created_at and nothing deletes standing — but revocation-by-delete is a property of a store and there is no store, so this is narrower than it reads |
| **PASS** | `standalone-suites` | every test file runs standalone and can fail | 32 test file(s): each has a __main__ runner that exits nonzero on failure |
| **PASS** | `ablation` | every guard shown to fail (rule 19) | all 140 guards ablate red; the suites are not decorative |
| **PASS** | `exit-line` | the exit line and its export (§11.1) | docs/EXIT.md, 76 lines |
| **PASS** | `component-map` | §14 says whether anyone looked (item 0) | §14: 10/40 exists-rows verified at source |
| **UNKNOWN** | `declared-sockets` | listening sockets are declared (§4.3, item 4 note ii) | no manifest and no listeners: nothing was checked. The checker is wired and shown to fail (tests/fixtures/decoys), so the first surface item 4 lands is caught on arrival |
| **PASS** | `anchor-payload` | an anchor crosses because it carries nothing else (§5, §6) | three attempts made and refused (a subclass field, an attribute hung on the instance, a publication off the calendar); an ordinary anchor yields 32 bytes |
| **UNKNOWN** | `anchor-published` | anchors are published on the calendar (§18 item 15) | the payload, the register and the pending states exist and are shown to fail (tests/test_publication.py); whether a deployment submits weekly is a property of a seam this tree deliberately does not contain |
| **ABSENT** | `receipt-attribution` | guardian receipts a third party can attribute (§18 item 15) | the only issuance scheme here is hmac-sha256, which is self_verifiable: the programme can check its own receipts and a third party cannot attribute one. The seam (records.receipts.Signer) is built and no primitive is wired |
| **PASS** | `deposit-procedure` | the annual deposit is written down (§18 item 15) | docs/WITNESS-DEPOSIT.md, 147 lines, all 5 required sections present. Whether a deposit was performed is not decidable from a tree |
| **UNKNOWN** | `manifest` | manifest with a build-failing cloud-permission check (§6) | no manifest exists in this repository; §18 item 4 has to say what surfaces exist before one can declare them |
| **UNKNOWN** | `security-audit` | SECURITY_AUDIT.md against the shared rubric (§10) | willow-2.0's 15-check rubric is named in §14 as reusable and has not been run here; UNVERIFIED at source |
| **UNKNOWN** | `sidecar-only` | canonical store read-only; agent writes are sidecar (§5) | there is no store, so the rule cannot be violated or demonstrated |
| **UNKNOWN** | `knock-enforcing` | the knock wired in enforcement mode (§7.2) | session reconciliation lives in willow-gate and is not wired here |
| **UNKNOWN** | `allowlist-rot` | allowlist rot tests (§16) | no destination allowlist exists; §14 records the fleet-wide version as unique to UTETY |
| **UNKNOWN** | `named-middles` | a named middle for every pair the app creates (§16) | not mechanically decidable. Five middles are named and tested (crossing, standing.is_self_edge, marking.drift, practice._one_lane, publication.reconcile); whether that is *every* pair is a reading, not a check |

---

Written by `tools/conform.py`. Records are never overwritten: *"does it conform"* is answerable from any one of them, and *"when did it stop"* only from the series.
