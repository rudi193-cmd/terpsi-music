# Conformance record — 2026-08-02

- **at** `2026-08-02T02:02:20+00:00`
- **commit** `6f6f4b1cd43a`
- **outcome digest** `3e1ab827934a9642`

**17 pass · 0 fail · 7 unknown · 0 absent** of 24 checks.

`UNKNOWN` is not a pass. A record that read all-pass on the day it was first written would be the formality §17 warns about, not a check.

| | check | §17 guarantee | evidence |
|---|---|---|---|
| **PASS** | `no-egress` | core purity: the inner ring cannot reach out | 29 module(s) scanned recursively by AST; no egress import, no dynamic import of one, no process spawn |
| **PASS** | `write-paths` | declared write paths (§6, gate G-C) | 29 module(s) in the inner ring scanned by AST: no writes, no reads; 4 write path(s) declared in manifest.json and reconciled against 161 scanned module(s), both directions |
| **UNKNOWN** | `dated-revocation` | revocation by date, never by delete (§7.1) | 29 module(s): Edge carries valid_at/invalid_at/created_at and nothing deletes standing — but revocation-by-delete is a property of a store and there is no store, so this is narrower than it reads |
| **PASS** | `standalone-suites` | every test file runs standalone and can fail | 69 test file(s): each has a __main__ runner that exits nonzero on failure |
| **PASS** | `ablation` | every guard shown to fail (rule 19) | all 487 guards ablate red; the suites are not decorative |
| **PASS** | `exit-line` | the exit line and its export (§11.1) | docs/EXIT.md, 76 lines |
| **PASS** | `component-map` | §14 says whether anyone looked (item 0) | §14: 41/41 exists-rows verified at source |
| **PASS** | `classification-registry` | the class-to-L mapping is enforced in one place (§9 item 2, §16) | 120 field(s): the seed, records/classify.py and SENSITIVITY.md's table agree (10 elevated by a recorded rule) |
| **PASS** | `row-security-differential` | one read predicate, two implementations, one middle (§16, rule 12) | 14 case(s) driven through both layers through records/serving.py with real rows and through the cluster under RLS; no disagreement in either direction |
| **PASS** | `knock-enforcing` | the knock wired in enforcement mode (§7.2) | test_store_knock: 6/6 passed — a session refused a read without a declared purpose and reconciled declared against observed on exit, landing the reconciled_session row (console/session.py, store/reconcile.py) |
| **PASS** | `declared-sockets` | listening sockets are declared (§4.3, item 4 note ii) | 161 source file(s) scanned; 0 listener(s) and 3 outbound connection(s), every one declared |
| **PASS** | `manifest` | manifest with a build-failing cloud-permission check (§6) | 4 surface(s) declared and present, 161 source file(s) reconciled: no listener, no outbound, no cloud permission, no unknown key |
| **UNKNOWN** | `key-escrow` | data at rest is sealed and its key escrowed (§5; §10's R16) | docs/ESCROW.md: policy recorded, threshold 3-of-5, 5 custodian(s), 0 rehearsal(s) on record. That is records/atrest.py's UNKNOWN — recorded and never rehearsed — and §5 is why it is not a pass: an untested key recovery is not escrow. 1 column(s) seal at rest (lane_entry.payload), so a sealed store can exist here, so this is not ABSENT either. The first rehearsal is an install-acceptance act (§11.1); the sealing primitive is usable |
| **PASS** | `security-audit` | the fifteen-check rubric run here, as a gate (§10) | SECURITY-AUDIT.md dated 2026-08-01 at `106ec133ffec12d8981cb449938b9fddd4e0bf13`, 1 day(s) old; 5 finding(s) recorded, 0 open, none at S1 or above |
| **PASS** | `stdlib-only` | every import is stdlib, declared, or local (TM-DEPS-01, §10) | 162 file(s): every import stdlib / declared / local; the two declared roots are cryptography and psycopg |
| **PASS** | `trust-root` | the trust root is never committed (TM-ROOT-01, refusal 2, §6) | 304 tracked path(s); none is trust-root material |
| **UNKNOWN** | `local-inference` | every inference path routes through the refusal-1 guard (§6) | 47 module(s) scanned by AST and none reaches a model: nothing was checked. The guard (records/inference.py) and this tripwire are wired and shown to fail against tests/fixtures/decoys, so the first inference path is caught on arrival |
| **PASS** | `anchor-payload` | an anchor crosses because it carries nothing else (§5, §6) | three attempts made and refused (a subclass field, an attribute hung on the instance, a publication off the calendar); an ordinary anchor yields 32 bytes |
| **UNKNOWN** | `anchor-published` | anchors are published on the calendar (§18 item 15) | the payload, the register and the pending states exist and are shown to fail (tests/test_publication.py); whether a deployment submits weekly is a property of a seam this tree deliberately does not contain |
| **PASS** | `receipt-attribution` | guardian receipts a third party can attribute (§18 item 15) | issuance schemes available: ed25519, hmac-sha256 |
| **PASS** | `deposit-procedure` | the annual deposit is written down (§18 item 15) | docs/WITNESS-DEPOSIT.md, 147 lines, all 5 required sections present. Whether a deposit was performed is not decidable from a tree |
| **UNKNOWN** | `sidecar-only` | canonical store read-only; agent writes are sidecar (§5) | **this row's old evidence — 'there is no store, so the rule cannot be violated or demonstrated' — stopped being true on 2026-07-31.** There is one, and half the rule is now enforced: store/writing.py lands application writes as drafts and cannot spell 'sealed', the app role holds INSERT and SELECT and no UPDATE or DELETE, and migration 002 makes a sealed row rewritable by nobody. What is still unbuilt is the other half — nothing yet *promotes* a draft, because the seal is records/sealing.py's and S-3 wires it. So the cascade has a floor and no ceiling: rule 18 says that is a partial enforcement and not a pass, and this row stays UNKNOWN until a named human's seal is what moves a row |
| **UNKNOWN** | `allowlist-rot` | allowlist rot tests (§16) | no destination allowlist exists; §14 records the fleet-wide version as unique to UTETY |
| **UNKNOWN** | `named-middles` | a named middle for every pair the app creates (§16) | not mechanically decidable. 25 middles are named and tested (crossing, standing.is_self_edge, standing.may_supersede, serving._acting_ward, serving._ceiling, marking.drift, practice._one_lane, publication.reconcile, inference.CLASSES<->SENSITIVITY.md, inference.COVERED_CLASSES<->CLAUDE.md refusal 1, providers.GUARD_ENTRIES<->records.inference, scales.drift, render.check, manifest.reconcile, registry.reconcile<->migrations/001_lanes.sql+records/classify.py, atrest.reconcile, atrest.composes, venue.sourcing.divergence<->records.marking P1-P5, commentary.SA3_CLAUSE<->§13, commentary.GuestSession<->reconciled_session, aggregate._descriptor_for<->classify step 2a, aggregate._legitimate<->rule 9 gate, test_store_differential<->serving.serve+migrations/003_row_security.sql, imports.declared_roots<->requirements.txt, checks.diff_declined<->prose.run_diff); whether that is *every* pair is a reading, not a check |

---

Written by `tools/conform.py`. Records are never overwritten: *"does it conform"* is answerable from any one of them, and *"when did it stop"* only from the series.
